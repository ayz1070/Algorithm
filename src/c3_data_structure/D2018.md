# 1. 문제 분석하기
- 최대 n = 10000000 이므로 O(n)의 시간복잡도로 접근
- 15를 표현하는 연속된 수 : 15, 7+8, 4+5+6, 1+2+3+4+5 총 4가지

# 2. 문제 접근하기
- 투 포인터 O(n)

# 3. 수도 코드
~~~
long n 입력
int sum = 1;
int count =1;

while(end != n){
   if(sum==n){
      count +=1; end++; sum += end;
   }else if(sum<n){
      end++; sum += end;
   }else if(sum>N){
      sum -+ start; start+=1;
   }
}