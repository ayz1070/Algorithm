# 1. 문제 분석하기
- 1억번의 연산 허용, n은 최대 100이므로 시간복잡도는 크게 상관 없음
- 100자리의 숫자를 int 형으로 담을 수 없으므로 String으로 처리

# 2. 문제 접근하기
1. n을 입력
2. String 타입의 숫자 문자열 입력
3. Char 타입의 배열로 쪼갠 후 int 형으로 변환하여 합 계산

# 3. 수도코드
~~~
n 입력
numString 입력
numChars[] 배열로 변환
for(n만큼){
    sum += numChars[i] - '0'
} 
print(sum)
~~~